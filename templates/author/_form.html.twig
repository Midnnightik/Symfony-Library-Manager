{{ form_start(form, {'attr': {'class': 'card p-3 shadow-sm bg-white'}}) }}
  {{ form_row(form.firstName) }}
  {{ form_row(form.lastName) }}
  {{ form_row(form.bio) }}
  {{ form_row(form.createdAt) }}
  <div class="mt-3">
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
  </div>
{{ form_end(form) }}
